#version 460

layout(local_size_x = 32, local_size_y = 32, local_size_z = 1) in;

layout(set=0, binding=0, r32f) uniform image2DArray tex;

void main() {
    imageStore(tex, ivec3(gl_GlobalInvocationID.xy,0), vec4(10,10,10,10));
}